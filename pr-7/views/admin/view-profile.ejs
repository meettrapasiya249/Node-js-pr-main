<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>View Profile</title>
    <link href="/dist/css/style.min.css" rel="stylesheet">
</head>

<body class="bg-dark">

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card shadow-lg">
                <div class="card-body text-center">

                    <img
                        src="<% if(currentAdmin.profileImg){ %>/uploads/<%= currentAdmin.profileImg %><% } else { %>/assets/images/users/1.jpg<% } %>"
                        class="rounded-circle mb-3"
                        width="120"
                        height="120"
                    >

                    <h4><%= currentAdmin.adminName %></h4>
                    <p class="text-muted"><%= currentAdmin.email %></p>

                    <hr>

                    <p><strong>Mobile:</strong> <%= currentAdmin.mobile || 'N/A' %></p>
                    <p><strong>Gender:</strong> <%= currentAdmin.gender || 'N/A' %></p>
                    <p><strong>Created On:</strong> <%= currentAdmin.date || 'N/A' %></p>

                    <a href="/admin/edit-profile/<%= currentAdmin._id %>"
                       class="btn btn-primary mt-3">
                        Edit Profile
                    </a>

                </div>
            </div>

        </div>
    </div>
</div>

</body>
</html>