<!DOCTYPE html>
<html>
<head>
  <title>Edit Movie</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="form-container">
    <h1>Edit Movie</h1>
    
    <div style="text-align: center; margin-bottom: 20px;">
      <p style="font-size: 0.8rem; color: var(--text-muted);">Current Poster:</p>
      <img src="/uploads/<%= movie.img %>" width="120" style="border-radius: 12px; margin-top: 10px; box-shadow: var(--shadow);">
    </div>

    <form action="/edit/<%= movie._id %>" method="POST" enctype="multipart/form-data">
      <label>Movie Title</label>
      <input type="text" name="title" value="<%= movie.title %>" required>
      
      <label>Genre</label>
      <input type="text" name="genre" value="<%= movie.genre %>" required>
      
      <label>Rating</label>
      <input type="number" step="0.1" name="rating" value="<%= movie.rating %>" required>
      
      <label>Release Year</label>
      <input type="number" name="releaseYear" value="<%= movie.releaseYear %>" required>
      
      <label>Change Poster (Optional)</label>
      <input type="file" name="img">
      
      <button type="submit">Update Movie Details</button>
      <a href="/" style="display:block; text-align:center; margin-top:15px; color:var(--text-muted); text-decoration:none;">Cancel Edit</a>
    </form>
  </div>
</body>
</html>